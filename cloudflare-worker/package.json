{
  "name": "owaua-cloudflare-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test test/*.test.js",
    "dry-run": "wrangler deploy --dry-run --outdir .wrangler-dry-run"
  },
  "devDependencies": {
    "wrangler": "4.124.0"
  },
  "engines": {
    "node": ">=22"
  }
}
